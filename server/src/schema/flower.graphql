"""
Root GraphQL schema file.
Author: Andrew Jarombek
Date: 5/18/2020
"""

extend type Query {
    flower(id: ID!): Flower
}

type Flower {
    id: ID!,
    name: String!,
    url: String!,
    lifespan: FlowerLifespan!,
    inStock: Boolean,
    onSale: Boolean,
    count: Int
    price: Float,
    salePrice: Float,
    description: String
}

enum FlowerLifespan {
    ANNUAL, PERENNIAL, SHRUB
}
