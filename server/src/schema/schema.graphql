"""
Root GraphQL schema file.
Author: Andrew Jarombek
Date: 5/18/2020
"""

type Query {
    ping: Int,
    flower(id: ID!): Flower
}

type Flower {
    id: ID!,
    name: String!,
    url: String!,
    lifespan: FlowerLifespan!,
    inStock: Boolean,
    count: Int
    price: Float,
    description: String
}

enum FlowerLifespan {
    ANNUAL, PERENNIAL
}
